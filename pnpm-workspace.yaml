packages:
  - packages/*
  - apps/*

# esbuild (pulled in by drizzle-kit) works from its prebuilt platform package;
# its install script is not needed, so it is not allowed to run.
allowBuilds:
  esbuild: false
